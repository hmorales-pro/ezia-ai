version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.fix-tailwind-absolute
      args:
        - BUILDKIT_PROGRESS=plain
    ports:
      - "${PORT:-3000}:3000"
    environment:
      # Mode production forcé
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
      
      # Configuration MongoDB
      - MONGODB_URI=${MONGODB_URI:-mongodb://localhost:27017/ezia}
      
      # Tokens API
      - HF_TOKEN=${HF_TOKEN}
      - DEFAULT_HF_TOKEN=${DEFAULT_HF_TOKEN}
      - MISTRAL_API_KEY=${MISTRAL_API_KEY}
      
      # Secrets
      - JWT_SECRET=${JWT_SECRET:-your-jwt-secret}
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET:-your-nextauth-secret}
      
      # URLs publiques pour HTTPS
      - NEXT_PUBLIC_APP_URL=${NEXT_PUBLIC_APP_URL:-https://ezia.ai}
      
    restart: unless-stopped
    
    # Logs verbeux pour debug
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"